httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
# IPO Porto tem anti-bot em JS; precisa de um browser real (ver common/browser.py).
# Depois de instalar: python3 -m playwright install chromium  (ou usar GOOGLE_CHROME_BIN)
# e ter o Xvfb disponível (pacote xvfb) para correr headful sem ecrã.
playwright>=1.40
